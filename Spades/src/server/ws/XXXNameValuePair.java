package server.ws;

/*
 * This isn't actually used and can be deleted from server.ws...
 */
public interface XXXNameValuePair {
	public String getname(int index);
	public String getvalue(int index);
}
